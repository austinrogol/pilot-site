# edgeloop II — runtime dependencies
#
# Pinned to lower bounds; the versions actually resolved during phase 1 are
# recorded in docs/PHASE1_REPORT.md so a rebuild is reproducible.

fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6

SQLAlchemy>=2.0
alembic>=1.13

numpy>=1.26
scipy>=1.11
pandas>=2.1
arch>=6.3          # Sheppard's GARCH; gives parameter standard errors (constraint 3)

# tests
pytest>=8.0
pytest-asyncio>=0.23
